<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="she.why.mapper.LoginMapper">
    <insert id="insertRegisterBlog" parameterType="she.why.entity.UserBlogEntity">
        insert into
        user_blog
        (id,
        user_id,
        user_name,
        password,
        email,
        phone_num,
        create_date,
        update_date,
        update_name)
        values
        #{userId},
        #{userName},
        #{password},
        #{email},
        #{phoneNum},
        #{createDate},
        #{updateDate},
        #{updateName}
    </insert>
</mapper>